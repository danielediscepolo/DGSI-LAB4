# Week 04 - Function Calling Loop (wget + SQLite)

## Run

```bash
python tool_loop_agent.py
```

Optional flags:

```bash
python tool_loop_agent.py --prompt "Create table test(id INTEGER, name TEXT)" --confirm-policy always
python tool_loop_agent.py --confirm-policy never
```
