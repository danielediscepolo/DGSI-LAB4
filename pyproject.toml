[project]
name = "week-04-function-calling-loop"
version = "0.1.0"
description = "Week 4 exercise: multi-round function calling with wget and sqlite"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
